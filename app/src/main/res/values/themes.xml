<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DebtNote" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary500</item>
        <item name="colorOnPrimary">@color/neutral</item>
        <item name="colorOnBackground">@color/neutralsoft</item>
    </style>

    <style name="Theme.DebtNote" parent="Base.Theme.DebtNote" />
</resources>